# Claude Code Project Memory

## Communication Style
- Be concise and to the point. Avoid verbose explanations.
- Skip lengthy summaries of what was found or what will be done — just do it.

## Project Info
- Flutter Web PWA deployed to Azure Static Web Apps (release branch) and GitHub Pages (main branch).
- Uses a version.json-based forced update mechanism in web/index.html.
- No-cache headers set globally in staticwebapp.config.json.
